part of '../room_inspection_screen.dart';

class ToiletInspectionForm extends StatelessWidget {
  const ToiletInspectionForm({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Toilet Inspection Form'),
      ),
    );
  }
}
