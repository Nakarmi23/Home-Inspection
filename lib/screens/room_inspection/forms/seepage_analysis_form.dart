part of '../room_inspection_screen.dart';

class SeepageAnalysisFrom extends StatelessWidget {
  const SeepageAnalysisFrom({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Seepage Analysis Form'),
      ),
    );
  }
}
