part of '../room_inspection_screen.dart';

class WaterQualityForm extends StatelessWidget {
  const WaterQualityForm({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Water Quality Form'),
      ),
    );
  }
}
