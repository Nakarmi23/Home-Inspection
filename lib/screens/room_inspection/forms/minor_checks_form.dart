part of '../room_inspection_screen.dart';

class MinorChecksForm extends StatelessWidget {
  const MinorChecksForm({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Minor Checks Form'),
      ),
    );
  }
}
